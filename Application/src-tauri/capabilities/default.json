{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window. Needs core:event so the frontend can receive the snapshot and log events the core pushes; the app's own #[tauri::command]s are covered by core:default.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
