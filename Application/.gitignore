# Node
node_modules/

# Vite output
dist/

# Rust / Tauri build output
src-tauri/target/

# Tauri regenerates most of this, but the Android project is CUSTOMISED --
# the manifest, the alarm receiver and the release cleartext setting all live
# there, so it must be tracked. Only its build output is ignored.
src-tauri/gen/apple/
src-tauri/gen/android/.gradle/
src-tauri/gen/android/build/
src-tauri/gen/android/app/build/
src-tauri/gen/android/local.properties
src-tauri/gen/android/.idea/

# Local overrides
.env
.env.local
