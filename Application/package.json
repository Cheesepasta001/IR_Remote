{
  "name": "ir-remote",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "mock": "node mock/server.js",
    "test": "vitest run",
    "check:secrets": "node scripts/check-no-secrets.mjs",
    "verify": "npm run build && npm run test && npm run check:secrets",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2"
  }
}
